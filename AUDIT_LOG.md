| File | Manifestation | Root Cause | Correction | Commit Hash |
|------|---------------|------------|------------|-------------|
| `data.py` | `FileNotFoundError` when loading datasets | Hardcoded `_data.pt` suffix appended to dataset name (e.g., looking for `orgs_data.pt` instead of `orgs.pt`) | Changed `d_path = Path(data_path) / f"{data}_data.pt"` to `d_path = Path(data_path) / f"{data}.pt"` | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `data.py` | Validation data was a subset of training data, causing data leakage and unrealistic performance metrics | Training data was not sliced; included all samples including those meant for validation | Added `[:val_start]` slicing to training data: `train_data = data_dict['train_images'][:val_start]` and `train_labels = data_dict['train_labels'][:val_start]` | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `fit.py` | Gradients accumulated across batches, leading to incorrect weight updates and unstable training | Missing `zero_grad()` call before `loss.backward()`. PyTorch accumulates gradients by default | Added `self.optimizer.zero_grad()` before `loss.backward()` in the training loop | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `fit.py` | Criterion failed with shape mismatch errors (broadcasting issues) during training and evaluation | Labels had shape `(batch_size, 1)` but `CrossEntropyLoss` expected `(batch_size,)` | Added `labels = labels.flatten()` in both `train_one_epoch()` and `evaluate()` | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `models.py` | Network acted entirely linear; model failed to learn complex patterns | `activation_str = "Identity"` removed all non-linearity from the network | Changed `activation_str = "ReLU"` to introduce non-linearity | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `models.py` | `ResNet18.forward()` returned `None`, causing the model to crash or produce no output | Missing `return` statement at the end of the forward function | Added `return self.classifier(out)` at the end of `ResNet18.forward()` | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `models.py` | Model failed on datasets with different channels or class counts (e.g., chest, orgs) | Hardcoded 3 input channels and 11 output classes in AlexNet | Added `in_channels` and `num_classes` as dynamic parameters; updated Conv2d and Linear layers accordingly | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `models.py` | Dimension mismatch crash during AlexNet forward pass | Classifier expected 2048 features, but convolutional layers output 3072 features (`192 * 4 * 4`) | Changed `nn.Linear(2048, 1024)` to `nn.Linear(3072, 1024)` | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `models.py` | Spatial dimensions inflated incorrectly in VGG blocks | 1x1 kernels used padding=1, adding borders and increasing spatial size | Added conditional padding: `pad = 0 if is_config_c_tail else padding` | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `models.py` | Channel mismatches in VGGBlock convolutions | `current_in_channels` was not updated after each convolution layer | Added `current_in_channels = out_channels` after each convolution | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `train.py` | Model failed to learn; training loss did not decrease | Dropout rate of 0.99 deactivated 99% of neurons, blocking information flow | Reduced dropout rate from 0.99 to 0.3 | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |
| `train.py` | Model had reduced expressiveness and non-linearity | `activation_str=None` resulted in Identity activation (no non-linearity) | Added `activation_str="ReLU"` when instantiating models | `b87c60f1c4e386a354cd25a8265e16bd9efbfd26` |